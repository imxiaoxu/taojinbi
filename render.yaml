services:
  - type: web
    name: coin-game-agent-sandbox-api
    runtime: node
    plan: free
    rootDir: deployment/render_backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false

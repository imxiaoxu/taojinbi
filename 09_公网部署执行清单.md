# 公网部署执行清单

## 当前结论

GitHub 仓库与 Render Backend 已完成部署，Render 健康检查已通过。Cloudflare Pages 尚待项目所有者登录 Cloudflare 后完成发布；获得最终 `pages.dev` 地址后，还需收紧 Render CORS 并执行 18 项公网验收。

| 项目 | 当前状态 | 地址 |
|---|---|---|
| GitHub 仓库 | 已完成 | <https://github.com/imxiaoxu/taojinbi> |
| Render Backend | 已完成 | <https://coin-game-agent-sandbox-api.onrender.com> |
| Cloudflare Pages | 待发布 | 登录 Cloudflare 后生成 |

## 一次性发布顺序

### 1. 建立 GitHub 仓库

本项目已将 `Week6_AB监控预测交付包` 作为仓库根目录提交至：

```text
https://github.com/imxiaoxu/taojinbi
```

### 2. 发布 Render Backend

1. Render 选择 `New -> Blueprint`。
2. 连接上一步仓库，读取根目录 `render.yaml`。
3. 首次将 `ALLOWED_ORIGINS=*`，部署成功后的服务地址为：

```text
https://coin-game-agent-sandbox-api.onrender.com
```

4. `/health` 已验证返回 `status=ok`、`mock_only=true`。

### 3. 构建并发布 Cloudflare Pages

在 Cloudflare Pages 连接同一仓库，配置：

| 配置项 | 值 |
|---|---|
| Framework preset | None |
| Build command | `node deployment/build_cloudflare.mjs` |
| Build output directory | `deployment/cloudflare_dist` |
| `RENDER_API_URL` | `https://coin-game-agent-sandbox-api.onrender.com` |

部署成功后记录：

```text
https://<cloudflare-project>.pages.dev
```

### 4. 收紧 CORS

回到 Render，将 `ALLOWED_ORIGINS` 从 `*` 改为最终 Pages 地址并重新部署。若同时配置自定义域名，用英文逗号分隔。

### 5. 公网自动验收

```bash
RENDER_URL=https://coin-game-agent-sandbox-api.onrender.com \
PAGES_URL=https://<cloudflare-project>.pages.dev \
node deployment/public_smoke_test.mjs
```

验收范围：

- Render 健康检查与 `mock_only` 边界；
- 9 个预设场景；
- Agent、固定规则、Holdout 三组七阶段 Pipeline；
- Cloudflare 首页与 Render 地址绑定；
- 公网 CSV 下载。

只有脚本输出 `Public deployment smoke tests: 18/18 passed` 后，Pages 地址才可作为正式测试链接提交。

## 最终交付信息

```text
项目测试地址：https://<cloudflare-project>.pages.dev
后端健康检查：https://coin-game-agent-sandbox-api.onrender.com/health
测试入口：Agent 决策测试 -> 在线测试
数据边界：合成数据 + Mock 工具，不接入真实淘金币业务。
```
